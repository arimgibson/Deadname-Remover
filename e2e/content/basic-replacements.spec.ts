import { test, expect } from '../fixtures'
import { createTestNameConfig, setupTestConfig } from '../helpers/setupTestConfig'

test.fixme('basic replacement on Google Search', async ({ page, extensionId }) => {
})

test.fixme('basic replacement on DuckDuckGo', async ({ page, extensionId }) => {
})
